[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "==2.25.1"
slack-sdk = ">=3.0"
slackeventsapi = ">=2.1.0"
Flask = ">=1.1.2"
ansible = "==4.2.0"
openshift = "==0.11.0"
kubernetes = "==11.0.0"

[dev-packages]

[requires]
python_version = "3.8"
